# PowerShell Automation

Planned scripts:

- `New-Employee.ps1`
- `Disable-Employee.ps1`
- `Endpoint-HealthCheck.ps1`

Scripts will be added only when their manual workflows are understood and can be tested safely. Each script must support validation, clear errors, non-secret logging and a safe preview or confirmation path where practical.

